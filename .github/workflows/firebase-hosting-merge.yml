# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Initilization, Build, and Deploy to Firebase Hosting

on:
  push:
    branches:
      - main

jobs:
  Initilization:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Install Dependencies
        run: npm ci

      - name: Log Initilization Job Completed
        run: echo "********* Initilization Completed! *********"

  build:
    needs: Initilization
    runs-on: ubuntu-latest
    steps:
      - name: Build Project
        run: npm run build

      - name: Log Build Job Completed
        run: echo "********* Build Completed! *********"

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps: 

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_YASWANTH_PANGULURI_ATS_10562 }}'
          channelId: live
          projectId: yaswanth-panguluri-ats-10562

      - name: Log Deploy Job Completed
        run: echo "********* Deployment Completed! *********"
